# pnpm hoisting strategy
# shamefully-hoist=true uses flat node_modules (like npm) instead of symlinks.
# Required for Docker builds: BuildKit's overlay filesystem cannot replace a
# cached directory (node_modules/@nexustreasury/domain) with a workspace symlink
# when multiple matrix jobs share the same BuildKit cache layer.
shamefully-hoist=true
